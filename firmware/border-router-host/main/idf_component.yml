# Thread Border Router — components nhúng trong project (không clone ngoài)
dependencies:
  espressif/mdns: "^1.10.1"
  espressif/esp_ot_cli_extension:
    version: "~2.0.1"

idf:
  version: ">=5.1.0"
