# Thread Border Router — components embedded in the project (no external clone)
dependencies:
  espressif/mdns: "^1.10.1"
  espressif/esp_ot_cli_extension:
    version: "~2.0.1"

idf:
  version: ">=5.1.0"
